import assert from "node:assert/strict";
import { readFileSync } from "node:fs";

const html = readFileSync(new URL("../index.html", import.meta.url), "utf8");
const css = readFileSync(new URL("../style.css", import.meta.url), "utf8");
const app = readFileSync(new URL("../app.js", import.meta.url), "utf8");
const ids = [...html.matchAll(/\sid="([^"]+)"/g)].map(match => match[1]);
assert.equal(new Set(ids).size, ids.length, "HTML IDs must remain unique");
for (const id of ["quickTraceBar", "quickOriginalButton", "quickAssistButton", "quickTraceExpandButton", "traceModeChips", "traceDetailInput", "tracePriorityInput", "traceQuality", "traceGuideOverlay", "traceGuideEnabledInput", "traceGuideModeInput", "traceGuideStatus", "layersToggle", "adjustSheet", "sheetHandle", "presetDescription", "freezeFrame", "freezeCameraButton", "captureDrawingButton", "compareDrawingButton", "comparisonOverlay", "guidedTracingPanel", "regionOverlay", "regionTracingPanel", "sessionReplayPanel"]) assert.ok(html.includes(`id="${id}"`) || app.includes(id), `missing ${id}`);
assert.match(css, /100dvh|--app-viewport-height/);
assert.match(css, /safe-area-inset-bottom/);
assert.match(css, /trace-mode-chips/);
assert.match(css, /trace-control-section/);
assert.match(css, /layer-card:not\(.layers-expanded\)/);
assert.match(css, /trace-readability/);
assert.match(css, /symmetry-vertical/);
assert.match(css, /symmetry-radial/);
assert.match(css, /data-state="half"/);
assert.match(app, /bindSheetDrag/);
assert.match(app, /setFreezeCamera/);
assert.match(app, /freezeCamera/);
assert.match(app, /LAYER_CARD_STATE_KEY/);
assert.match(app, /Requires local hand-landmark model/);
assert.match(app, /readabilityMode/);
assert.match(app, /layersExpanded = false/);
assert.match(app, /adjustSheet\.dataset\.state = nextState/);
assert.match(app, /createRegionState/);
assert.match(app, /sessionReplay/);
console.log("mobile UI static checks passed");
